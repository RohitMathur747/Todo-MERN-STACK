# Todo React App

Scaffolded Vite + React project.

Quick start (PowerShell):

```powershell
cd "h:\Todo MERN STACK\todo-react-app"
npm install
npm run dev
```

Build:

```powershell
npm run build
npm run preview
```
